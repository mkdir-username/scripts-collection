# Детекторы Ошибок - Краткая Справка v1.0.0

## 🎯 Назначение

Модульная система детекторов для точного определения мест ошибок в SDUI контрактах.

## 📦 Компоненты

### 1. Error Field Detector - Детектор Полей с Ошибками

**Что делает:** Определяет точное поле с ошибкой из сообщения валидатора

**Пример:**
```typescript
import { detectErrorField } from './detectors';

const result = detectErrorField(
  "Component ButtonView not found",  // Сообщение
  "components[0]"                    // Путь к объекту
);

// Результат:
// {
//   field: "type",
//   path: "components[0].type",
//   confidence: "high",
//   reason: "Component type error"
// }
```

**Когда использовать:** При обработке ошибок валидации для показа пользователю точного места проблемы.

---

### 2. JQ Integration - Интеграция с jq

**Что делает:** Выполняет сложные запросы к JSON через jq или встроенную реализацию

**Пример:**
```typescript
import { jq } from './detectors';

// Простой запрос
const types = await jq(contract, '.components[].type');

// Фильтрация
const buttons = await jq(
  contract,
  '.components | map(select(.type == "ButtonView"))'
);
```

**Когда использовать:** Для извлечения данных из сложных JSON структур.

**Требования:** jq желательно, но не обязательно (есть fallback)

---

### 3. JSONPath Integration - Интеграция с JSONPath

**Что делает:** Ищет элементы в JSON по стандартным JSONPath выражениям

**Пример:**
```typescript
import { queryJSONPath } from './detectors';

// Все авторы
const authors = queryJSONPath(data, '$.store.book[*].author');

// Рекурсивный поиск всех цен
const prices = queryJSONPath(data, '$..price');

// С фильтрацией
const cheap = queryJSONPath(data, '$.book[?(@.price < 10)]');
```

**Когда использовать:** Для поиска элементов по сложным критериям.

**Требования:** Нет зависимостей (встроенная реализация)

---

### 4. Path Converter - Конвертер Форматов Путей

**Что делает:** Конвертирует пути между разными форматами

**Поддерживаемые форматы:**
- JSON Pointer: `/components/0/type`
- Property Path: `components[0].type`
- JSONPath: `$.components[0].type`
- jq: `.components[0].type`
- Dot notation: `components.0.type`

**Пример:**
```typescript
import {
  toJSONPointer,
  toPropertyPath,
  normalizePath
} from './detectors';

toJSONPointer('components[0].type');
// => "/components/0/type"

toPropertyPath('/components/0/type');
// => "components[0].type"

normalizePath('components.0.type');
// => "components[0].type"
```

**Когда использовать:** При работе с разными системами, использующими разные форматы путей.

---

## 🚀 Быстрый старт

### Установка

```bash
cd /Users/username/Scripts/vscode-validator-v2.3.1
npm install
```

### Использование в коде

```typescript
// 1. Импортировать нужные функции
import {
  detectErrorField,
  toJSONPointer,
  jq,
  queryJSONPath
} from './detectors';

// 2. Использовать
const errorInfo = detectErrorField(errorMessage, errorPath);
const pointer = toJSONPointer(errorInfo.path);
const value = await jq(contract, `.${errorInfo.path}`);
```

### Получение метрик

```typescript
import { getAllMetrics, printDiagnosticReport } from './detectors';

// Получить все метрики
const metrics = getAllMetrics();
console.log(metrics);

// Напечатать полный отчет
await printDiagnosticReport();
```

---

## 📊 Метрики

Все детекторы собирают метрики:

- **Общее количество запросов**
- **Попадания/промахи кэша**
- **Процент попаданий в кэш**
- **Ошибки**
- **Среднее время выполнения**

**Пример:**
```typescript
import { getDetectorMetrics } from './detectors';

const metrics = getDetectorMetrics();
console.log(`Точность: ${
  metrics.successfulDetections / metrics.totalRequests * 100
}%`);
console.log(`Cache hit rate: ${metrics.cacheHitRate}%`);
```

---

## 💡 Полезные паттерны

### Паттерн 1: Полная обработка ошибки

```typescript
import {
  detectErrorField,
  toJSONPointer,
  jq,
  queryJSONPath
} from './detectors';

async function processError(message: string, path: string, contract: any) {
  // 1. Определяем поле
  const error = detectErrorField(message, path);

  // 2. Конвертируем путь
  const pointer = toJSONPointer(error.path);

  // 3. Получаем текущее значение
  const currentValue = await jq(contract, `.${error.path}`);

  // 4. Ищем все похожие поля
  const similar = queryJSONPath(contract, `$..${error.field}`);

  return {
    field: error.field,
    path: error.path,
    pointer,
    confidence: error.confidence,
    currentValue: currentValue.data,
    similarFields: similar.data
  };
}
```

### Паттерн 2: Безопасная работа с внешними инструментами

```typescript
import { jqWithFallback } from './detectors';

// Попытка через jq, fallback на простой код
const result = await jqWithFallback(
  contract,
  '.components | map(select(.type == "ButtonView"))',
  (c) => c.components.filter(comp => comp.type === 'ButtonView')
);
```

### Паттерн 3: Очистка ресурсов

```typescript
import { clearAllCaches, resetAllMetrics } from './detectors';

// После тестов или периодически
clearAllCaches();    // Очистить кэши
resetAllMetrics();   // Сбросить метрики
```

---

## 🔧 Настройка

### Опции для jq

```typescript
const result = await jq(data, query, {
  timeout: 3000,      // Таймаут 3 секунды
  compact: true,      // Компактный вывод
  raw: false,         // С кавычками
  forceFallback: false // Не использовать shell
});
```

### Опции для JSONPath

```typescript
const result = queryJSONPath(data, path, {
  maxDepth: 50,       // Макс. глубина рекурсии
  forceFallback: false,
  valuesOnly: true    // Только значения
});
```

### Опции для конвертера путей

```typescript
const pointer = toJSONPointer(path, {
  validate: true,     // Валидировать
  normalize: true,    // Нормализовать
  escape: true        // Экранировать
});
```

---

## 📖 Дополнительная информация

- **Полная документация:** `README.md`
- **Отчет о создании:** `DETECTORS_DELIVERY_REPORT_v1.0.0.md`
- **Исходный код:** `/src/detectors/*.ts`

---

## ⚡ Производительность

**С кэшированием:**
- Error Field Detector: ~0.01ms (50x ускорение)
- JQ Integration: ~0.01ms (2000x+ ускорение)
- JSONPath Integration: ~0.01ms (50-200x ускорение)
- Path Converter: ~0.01ms (10x ускорение)

**Memory:**
- ~100-500 bytes на запись в кэше
- ~200-400 bytes на метрики модуля

---

**Версия:** 1.0.0
**Дата:** 2025-10-07
**Статус:** ✅ Готово к использованию
